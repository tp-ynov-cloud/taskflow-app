apiVersion: v1
kind: # [...]
metadata:
  name: postgres
  namespace: # [...]
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
  clusterIP: None   # Headless service pour StatefulSet