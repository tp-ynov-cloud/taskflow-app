apiVersion: v1
kind: Service
metadata:
  name: task-service
  namespace: staging
spec:
  selector:
    app: task-service
  ports:
    - name: http
      port: 3002
      targetPort: 3002
      protocol: TCP
